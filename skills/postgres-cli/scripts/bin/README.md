# Prebuilt Binaries

This directory stores release binaries selected by `scripts/postgres-cli`:

- `postgres-cli-darwin-arm64`
- `postgres-cli-linux-x86_64`
- `postgres-cli-windows-x86_64.exe`

Maintainers refresh these binaries from GitHub Release artifacts.
